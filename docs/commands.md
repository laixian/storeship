# Command reference

Generated by `storeship docs` from the command definitions — do not edit by hand. Every command accepts the global flags. Positional arguments are in `<>`; optional parts in `[]`.

## Global flags
| Flags | |
|---|---|
| `--json` | machine-readable output on stdout; progress still goes to stderr |
| `--config` | config file; default: storeship.config.* found walking up from cwd |

## Commands

- [`init`](#init) — create storeship.config.json from the project and App Store Connect
- [`doctor`](#doctor) — check the machine, the config and the API key; explains each miss
- [`ship`](#ship) — archive → export IPA → upload to App Store Connect (the version number is yours to set beforehand)
- [`export`](#export) — export an existing .xcarchive to an IPA (resume here when export failed after a good archive)
- [`upload`](#upload) — upload an already exported IPA with altool
- [`release`](#release) — the whole thing: ship + version create + whatsnew + attach --wait + submit
- [`products`](#products) — subscription groups, subscriptions, prices, availability and app price from products.md: check, diff against ASC, push
- [`version`](#version) — App Store version records: status, create, whatsnew, attach, submit, cancel
- [`builds`](#builds) — recent builds and their processing state
- [`listing`](#listing) — store metadata and App Review information from a Markdown file: check limits, diff against ASC, push
- [`media`](#media) — screenshots and preview videos: status per locale × device, upload, create slots
- [`offer`](#offer) — subscription offer codes (no UI for these in App Store Connect)
- [`device`](#device) — register a device for development signing
- [`apps`](#apps) — apps in the account (to find the app id)
- [`analytics`](#analytics) — Analytics Reports API and daily sales (needs an Admin / Sales key)
- [`shots`](#shots) — App Store screenshots: validate, render from real screenshots + template, contact sheet, upload by device type
- [`preview`](#preview) — App Preview video: record the simulator, cut VFR footage into a spec-size film, check, upload
- [`reel`](#reel) — vertical social video: a simulator recording inside a designed card, audio aligned by frame timestamps
- [`sim`](#sim) — drive the booted simulator: tap / drag / screenshot / status bar / accessibility tree (idb first, CGEvent fallback)
- [`skill`](#skill) — agent skills that drive this tool (Claude Code format)
- [`docs`](#docs) — generate the command reference (Markdown) from the command tree; --check compares with a file

## `init`

create storeship.config.json from the project and App Store Connect

Usage: `storeship init [--project DIR] [--key-id ID --issuer-id ID [--key-path P]] [--bundle-id ID] [--force]`

| Flags | |
|---|---|
| `--project` | the Expo / iOS project directory, relative to the config root; default: the root itself |
| `--key-id` | App Store Connect API key id (with --issuer-id the app id and locales are looked up) |
| `--issuer-id` | App Store Connect issuer id |
| `--key-path` | path to the .p8; default ~/.appstoreconnect/private_keys/AuthKey_<KEY_ID>.p8 |
| `--bundle-id` | bundle id to look up; default: read from expo config |
| `--force` | overwrite an existing storeship.config.json |

## `doctor`

check the machine, the config and the API key; explains each miss

Usage: `storeship doctor`

## `ship`

archive → export IPA → upload to App Store Connect (the version number is yours to set beforehand)

Usage: `storeship ship [--skip-upload] [--force] [--quiet]`

| Flags | |
|---|---|
| `--skip-upload` | archive and export only; print the IPA path for `upload` |
| `--force` | build even when app.config and ios/ disagree on the version (i.e. prebuild was not run) |
| `--quiet` | do not stream xcodebuild / altool output |

## `export`

export an existing .xcarchive to an IPA (resume here when export failed after a good archive)

Usage: `storeship export <path.xcarchive> [--quiet]`

| Flags | |
|---|---|
| `--quiet` | do not stream xcodebuild output |

## `upload`

upload an already exported IPA with altool

Usage: `storeship upload <file.ipa> [--quiet]`

| Flags | |
|---|---|
| `--quiet` | do not stream altool output |

## `release`

the whole thing: ship + version create + whatsnew + attach --wait + submit

Usage: `storeship release <version> [--date YYYY-MM-DD] [--whatsnew DIR] [--archive PATH.xcarchive] [--build N] [--no-ship] [--no-submit] [--yes] [--quiet] [--force] [--timeout MIN]`

| Flags | |
|---|---|
| `--date` | scheduled release day; without it the release is manual after approval |
| `--whatsnew` | directory with <locale>.txt What's New files; default <whatsNew.dir>/<version>, silently skipped when absent |
| `--archive` | export + upload this existing .xcarchive instead of archiving again (resume here when export or upload failed) |
| `--build` | build number to attach; default: the one just built, or with --no-ship the newest in the account |
| `--no-ship` | skip archive / export / upload (the build is already in App Store Connect) |
| `--no-submit` | stop after attaching the build |
| `--yes` | skip the confirmation (required when not in a terminal) |
| `--quiet` | do not stream xcodebuild / altool output |
| `--force` | build even when app.config and ios/ disagree on the version |
| `--timeout` | minutes to wait for the build to become VALID; default 40 |

## `products`

subscription groups, subscriptions, prices, availability and app price from products.md: check, diff against ASC, push

Subcommands: [`check`](#products-check) · [`status`](#products-status) · [`diff`](#products-diff) · [`push`](#products-push) · [`pricepoints`](#products-pricepoints) · [`delete`](#products-delete)

### `products check`

parse the products file and check limits and references (offline)

Usage: `storeship products check`

### `products status`

what App Store Connect has: groups, subscriptions, state, base price, territories, review screenshot

Usage: `storeship products status`

### `products diff`

compare the products file with App Store Connect and print every change it would make; writes nothing

Usage: `storeship products diff`

### `products push`

apply the diff: create groups / subscriptions / localizations, set prices (base territory equalized everywhere), availability, review screenshot

Usage: `storeship products push [--yes]`

| Flags | |
|---|---|
| `--yes` | skip the confirmation (required when not in a terminal) |

### `products pricepoints`

price tiers Apple offers for a subscription in a territory (they are discrete; look before writing a price)

Usage: `storeship products pricepoints <productId> <TERRITORY> [--near AMOUNT]`

| Flags | |
|---|---|
| `--near` | show only tiers around this amount |

### `products delete`

delete a subscription that was never submitted (the only kind ASC lets you delete); an emptied group goes too

Usage: `storeship products delete <productId> --yes`

| Flags | |
|---|---|
| `--yes` | required: deleting is permanent |

## `version`

App Store version records: status, create, whatsnew, attach, submit, cancel

Subcommands: [`status`](#version-status) · [`create`](#version-create) · [`whatsnew`](#version-whatsnew) · [`attach`](#version-attach) · [`submit`](#version-submit) · [`cancel`](#version-cancel)

### `version status`

list versions with state and release schedule

Usage: `storeship version status`

### `version create`

create a version record; with a date it becomes a scheduled release

Usage: `storeship version create <version> [--date YYYY-MM-DD]`

| Flags | |
|---|---|
| `--date` | schedule the release for this day (at release.scheduledTime from the config); without it the release is manual after approval |

### `version whatsnew`

write What's New for every locale

Usage: `storeship version whatsnew <version> [--dir DIR | --file <locale>=<path> …] [--dry-run]`

| Flags | |
|---|---|
| `--dir` | directory holding <locale>.txt per configured locale; default: <whatsNew.dir>/<version> |
| `--file` | one locale from one file, repeatable: --file en-US=notes/en.txt |
| `--dry-run` | print what would be written and stop |

### `version attach`

attach a build (must be VALID); --wait polls until it is

Usage: `storeship version attach <version> [--build N] [--wait] [--timeout MIN]`

| Flags | |
|---|---|
| `--build` | build number (CFBundleVersion) to attach; default: the newest build in the account |
| `--wait` | poll every 30 s until the build is VALID (or, with --build, until it appears and is VALID) instead of failing |
| `--timeout` | minutes to keep waiting with --wait; default 30 |

### `version submit`

submit the version for review

Usage: `storeship version submit <version>`

### `version cancel`

withdraw the open review submission (one-way: the queue position is lost)

Usage: `storeship version cancel --yes`

| Flags | |
|---|---|
| `--yes` | required: cancelling is one-way and forfeits the review queue position |

## `builds`

recent builds and their processing state

Usage: `storeship builds`

## `listing`

store metadata and App Review information from a Markdown file: check limits, diff against ASC, push

Subcommands: [`check`](#listing-check) · [`diff`](#listing-diff) · [`push`](#listing-push)

### `listing check`

parse the listing file and check character limits (offline)

Usage: `storeship listing check`

### `listing diff`

compare the listing file with what ASC has for a version

Usage: `storeship listing diff <version>`

### `listing push`

write differing fields to ASC (name/subtitle on appInfo, the rest and the review information on the version)

Usage: `storeship listing push <version>`

## `media`

screenshots and preview videos: status per locale × device, upload, create slots

Subcommands: [`status`](#media-status) · [`upload`](#media-upload) · [`list`](#media-list) · [`mkset`](#media-mkset) · [`delete`](#media-delete)

### `media status`

how many screenshots / previews each locale × device slot has (and the set ids)

Usage: `storeship media status <version>`

### `media upload`

upload files into a set (order = display order)

Usage: `storeship media upload <screenshot|preview> <setId> <file> [file …]`

### `media list`

items in a set with their delivery state

Usage: `storeship media list <screenshot|preview> <setId>`

### `media mkset`

create an empty set for a locale (needed once per device type before uploading)

Usage: `storeship media mkset <screenshot|preview> <localizationId> <displayType>   e.g. preview <locId> IPHONE_67`

### `media delete`

delete one screenshot / preview by id

Usage: `storeship media delete <screenshot|preview> <id>`

## `offer`

subscription offer codes (no UI for these in App Store Connect)

Subcommands: [`list`](#offer-list) · [`new`](#offer-new) · [`csv`](#offer-csv) · [`off`](#offer-off)

### `offer list`

offers and code batches per configured product

Usage: `storeship offer list [--product ALIAS|ID]`

| Flags | |
|---|---|
| `--product` | one product only (alias from config or a subscription id); default: every configured product |

### `offer new`

create a free offer, issue one-time codes, download the CSV

Usage: `storeship offer new --name NAME [--product P] [--duration ONE_YEAR] [--periods 1] [--codes 500] [--expires YYYY-MM-DD] [--eligibility NEW,EXISTING] [--renew] [--out FILE]`

| Flags | |
|---|---|
| `--name` | offer name; must be unique per product |
| `--product` | alias from config or a subscription id; default: the first configured product |
| `--duration` | free period unit: THREE_DAYS, ONE_WEEK, TWO_WEEKS, ONE_MONTH, TWO_MONTHS, THREE_MONTHS, SIX_MONTHS, ONE_YEAR; default ONE_YEAR |
| `--periods` | how many of those units; default 1 |
| `--codes` | number of one-time codes to issue; default 500 |
| `--expires` | last day the codes can be redeemed (YYYY-MM-DD, at most ~6 months out); default today + 175 days |
| `--eligibility` | comma list of NEW, EXISTING, EXPIRED; default NEW |
| `--renew` | let the subscription auto-renew at full price when the free period ends (Apple's default; this tool defaults to off and it cannot be changed afterwards) |
| `--out` | CSV path; default offer-codes-<name>.csv |

### `offer csv`

re-download a batch as CSV

Usage: `storeship offer csv --batch ID [--out FILE]`

| Flags | |
|---|---|
| `--batch` | batch id from `offer list` |
| `--out` | CSV path; default offer-codes-<batch>.csv |

### `offer off`

deactivate an offer and all its batches (codes stop working immediately)

Usage: `storeship offer off --offer ID`

| Flags | |
|---|---|
| `--offer` | offer id from `offer list`; all its batches are deactivated too and existing codes stop working |

## `device`

register a device for development signing

Usage: `storeship device add <name> <udid>`

Subcommands: [`add`](#device-add)

### `device add`

register a device

Usage: `storeship device add <name> <udid>`

## `apps`

apps in the account (to find the app id)

Usage: `storeship apps`

## `analytics`

Analytics Reports API and daily sales (needs an Admin / Sales key)

Subcommands: [`request`](#analytics-request) · [`list`](#analytics-list) · [`fetch`](#analytics-fetch) · [`sales`](#analytics-sales)

### `analytics request`

ask Apple for a report snapshot (data arrives the next day)

Usage: `storeship analytics request [--access ONE_TIME_SNAPSHOT|ONGOING]`

| Flags | |
|---|---|
| `--access` | ONE_TIME_SNAPSHOT (default) or ONGOING |

### `analytics list`

requested reports and how many instances each has

Usage: `storeship analytics list`

### `analytics fetch`

download the newest instance of a report as a table

Usage: `storeship analytics fetch "<report name>" [--granularity DAILY|WEEKLY|MONTHLY]`

| Flags | |
|---|---|
| `--granularity` | DAILY (default), WEEKLY or MONTHLY |

### `analytics sales`

daily sales summary for a vendor number

Usage: `storeship analytics sales <vendorNumber> [YYYY-MM-DD]`

## `shots`

App Store screenshots: validate, render from real screenshots + template, contact sheet, upload by device type

Subcommands: [`check`](#shots-check) · [`render`](#shots-render) · [`upload`](#shots-upload) · [`seed`](#shots-seed)

### `shots check`

validate content, sources, crops for every device (all problems at once)

Usage: `storeship shots check [--device a,b] [--locale x,y]`

| Flags | |
|---|---|
| `--device` | comma list of device ids; default: every device the content lays out (or shots.devices) |
| `--locale` | comma list of locales; default: the configured ones |

### `shots render`

render the set (checks first); --sheet also writes a contact sheet per device × locale

Usage: `storeship shots render [--device a,b] [--locale x,y] [--only 1,2] [--sheet]`

| Flags | |
|---|---|
| `--device` | comma list of device ids |
| `--locale` | comma list of locales |
| `--only` | comma list of shot numbers to re-render |
| `--sheet` | also write a contact sheet per device × locale into the temp dir |

### `shots upload`

push rendered screenshots into App Store Connect by display type (new files only unless --replace)

Usage: `storeship shots upload <version> [--device a,b] [--locale x,y] [--replace] [--dry-run]`

| Flags | |
|---|---|
| `--device` | comma list of device ids |
| `--locale` | comma list of locales |
| `--replace` | delete every screenshot in the set first |
| `--dry-run` | show what would be uploaded, touch nothing |

### `shots seed`

run the project's demo-data script against the simulator (args passed through)

Usage: `storeship shots seed [-- args…]`

## `preview`

App Preview video: record the simulator, cut VFR footage into a spec-size film, check, upload

Subcommands: [`record`](#preview-record) · [`stop`](#preview-stop) · [`cut`](#preview-cut) · [`check`](#preview-check) · [`upload`](#preview-upload)

### `preview record`

start recording the booted simulator (stop with `preview stop`; never kill the process)

Usage: `storeship preview record <out.mov> [--device id | --udid U]`

| Flags | |
|---|---|
| `--device` | device id; its simulator name selects the booted simulator |
| `--udid` | target a simulator by UDID instead |

### `preview stop`

stop the recording cleanly (SIGINT), so the session is not leaked in CoreSimulator

Usage: `storeship preview stop`

### `preview cut`

segments → CFR parts → crossfade → fades (+ music) at the preview size for the device

Usage: `storeship preview cut <out.mp4> <file:start:dur[:p]>… [--device id | --size WxH] [--portrait] [--music f.wav] [--fps 30] [--xfade 0.5]`

| Flags | |
|---|---|
| `--device` | device id → the App Preview size for it (landscape for iPhone, portrait for iPad) |
| `--size` | explicit canvas WxH instead of --device |
| `--portrait` | with --device: use the portrait size |
| `--music` | audio file mixed under the film, faded in 1 s / out 1.5 s, cut to the film length |
| `--fps` | constant frame rate of the film; default 30 |
| `--xfade` | crossfade seconds between segments; default 0.5 |

### `preview check`

size / duration / fps / frame count against the App Preview spec

Usage: `storeship preview check <file> [--device id]`

| Flags | |
|---|---|
| `--device` | check against this device's preview type; without it any App Preview size passes |

### `preview upload`

upload a preview into the right slot for a locale × device (creates the slot if missing)

Usage: `storeship preview upload <version> <file.mp4> --device id --locale L [--replace]`

| Flags | |
|---|---|
| `--device` | device id → preview type / slot |
| `--locale` | ASC locale code |
| `--replace` | delete the previews already in the slot first |

## `reel`

vertical social video: a simulator recording inside a designed card, audio aligned by frame timestamps

Subcommands: [`card`](#reel-card) · [`make`](#reel-make) · [`pts`](#reel-pts)

### `reel card`

render the card layer (transparent hole for the video) to a PNG

Usage: `storeship reel card <out.png>`

### `reel make`

recording (+ audio) → mp4; renders the card fresh each time

Usage: `storeship reel make <recording.mov> <out.mp4> [--start s] [--duration s] [--audio f.wav --audio-t0 s] [--card card.png]`

| Flags | |
|---|---|
| `--start` | seconds into the recording to start; default 0 |
| `--duration` | seconds to keep; default: to the end |
| `--audio` | audio file to lay under the picture |
| `--audio-t0` | where the audio's t=0 falls on the recording's timeline (seconds); from `reel pts` |
| `--card` | use this card PNG instead of rendering one |

### `reel pts`

frame timestamps of a recording, and the steady-cadence runs (the first frame of one is where audio t=0 belongs)

Usage: `storeship reel pts <recording.mov> [--all]`

| Flags | |
|---|---|
| `--all` | print every frame, not only the steady runs |

## `sim`

drive the booted simulator: tap / drag / screenshot / status bar / accessibility tree (idb first, CGEvent fallback)

Usage: `storeship sim <which|tap|ltap|drag|shot|statusbar|ls|find|text> … [--profile iphone69] [--udid U]`

| Flags | |
|---|---|
| `--profile` | device id from the device table (picks the simulator by name); default sim.profile from the config, else the only booted simulator |
| `--udid` | target a simulator by UDID instead |

Subcommands: [`which`](#sim-which) · [`tap`](#sim-tap) · [`ltap`](#sim-ltap) · [`drag`](#sim-drag) · [`shot`](#sim-shot) · [`statusbar`](#sim-statusbar) · [`ls`](#sim-ls) · [`find`](#sim-find) · [`text`](#sim-text)

### `sim which`

input channel and target simulator

Usage: `storeship sim which`

Flags (inherited from `sim`):
| Flags | |
|---|---|
| `--profile` | device id from the device table (picks the simulator by name); default sim.profile from the config, else the only booted simulator |
| `--udid` | target a simulator by UDID instead |

### `sim tap`

tap at device logical points (portrait)

Usage: `storeship sim tap <x> <y>`

Flags (inherited from `sim`):
| Flags | |
|---|---|
| `--profile` | device id from the device table (picks the simulator by name); default sim.profile from the config, else the only booted simulator |
| `--udid` | target a simulator by UDID instead |

### `sim ltap`

tap at pixel coordinates measured on the rotated (landscape) screenshot

Usage: `storeship sim ltap <x> <y>`

Flags (inherited from `sim`):
| Flags | |
|---|---|
| `--profile` | device id from the device table (picks the simulator by name); default sim.profile from the config, else the only booted simulator |
| `--udid` | target a simulator by UDID instead |

### `sim drag`

drag between two device points

Usage: `storeship sim drag <x1> <y1> <x2> <y2>`

Flags (inherited from `sim`):
| Flags | |
|---|---|
| `--profile` | device id from the device table (picks the simulator by name); default sim.profile from the config, else the only booted simulator |
| `--udid` | target a simulator by UDID instead |

### `sim shot`

screenshot; give 270 for a landscape page

Usage: `storeship sim shot <out.png> [rotate]`

Flags (inherited from `sim`):
| Flags | |
|---|---|
| `--profile` | device id from the device table (picks the simulator by name); default sim.profile from the config, else the only booted simulator |
| `--udid` | target a simulator by UDID instead |

### `sim statusbar`

override the status bar to 9:41 / full battery / full signal

Usage: `storeship sim statusbar [--time 9:41]`

| Flags | |
|---|---|
| `--time` | clock text; default 9:41 |

Flags (inherited from `sim`):
| Flags | |
|---|---|
| `--profile` | device id from the device table (picks the simulator by name); default sim.profile from the config, else the only booted simulator |
| `--udid` | target a simulator by UDID instead |

### `sim ls`

accessibility elements (needs idb); optional substring filter

Usage: `storeship sim ls [pattern]`

Flags (inherited from `sim`):
| Flags | |
|---|---|
| `--profile` | device id from the device table (picks the simulator by name); default sim.profile from the config, else the only booted simulator |
| `--udid` | target a simulator by UDID instead |

### `sim find`

tap the nth element whose label contains the text (needs idb)

Usage: `storeship sim find <label> [nth]`

Flags (inherited from `sim`):
| Flags | |
|---|---|
| `--profile` | device id from the device table (picks the simulator by name); default sim.profile from the config, else the only booted simulator |
| `--udid` | target a simulator by UDID instead |

### `sim text`

type text into the focused field (needs idb)

Usage: `storeship sim text <string>`

Flags (inherited from `sim`):
| Flags | |
|---|---|
| `--profile` | device id from the device table (picks the simulator by name); default sim.profile from the config, else the only booted simulator |
| `--udid` | target a simulator by UDID instead |

## `skill`

agent skills that drive this tool (Claude Code format)

Subcommands: [`list`](#skill-list) · [`install`](#skill-install)

### `skill list`

skills shipped with this version

Usage: `storeship skill list`

### `skill install`

copy the skills into a project (default .claude/skills)

Usage: `storeship skill install [--to DIR] [--only NAME]`

| Flags | |
|---|---|
| `--to` | target directory; default <config root>/.claude/skills |
| `--only` | install one skill by name |

## `docs`

generate the command reference (Markdown) from the command tree; --check compares with a file

Usage: `storeship docs [--lang en|zh] [--check FILE]`

| Flags | |
|---|---|
| `--lang` | en (default) or zh |
| `--check` | compare with this file and exit 1 when it differs (for CI) |
